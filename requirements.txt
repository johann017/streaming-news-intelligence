# Core pipeline dependencies
# Pin minor versions for reproducibility; patch updates are safe

# Ingestion
feedparser==6.0.11
requests==2.32.3

# Processing
langdetect==1.0.9
nltk==3.9.1

# Clustering / ML
sentence-transformers==3.3.1
scikit-learn==1.6.1
numpy==2.2.1

# Summarization
transformers==4.47.1
torch==2.5.1          # CPU-only; GitHub Actions free runner has no GPU
# torch installs torchvision/torchaudio as extras — not needed, skip to save space

# Firebase
firebase-admin==6.6.0

# Testing
pytest==8.3.4
pytest-cov==6.0.0

# Code quality (optional, used in CI)
mypy==1.14.1
ruff==0.9.2
